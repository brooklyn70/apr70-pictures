---
import type { DispatchIssueDoc } from '../../lib/payload'

interface Props {
  indicia: DispatchIssueDoc['indicia']
  title?: string
}

const { indicia, title = 'DIS·PATCH' } = Astro.props
const parts = String(title).split('·')
---

<div class="masthead">
  <div class="masthead-row">
    <div class="masthead-left">{indicia.volume} · {indicia.number}</div>
    <div class="masthead-wordmark">
      {parts.map((p, i) => (
        <>
          {p}
          {i < parts.length - 1 && <span class="amp">·</span>}
        </>
      ))}
    </div>
    <div class="masthead-right">{indicia.season}</div>
  </div>
  <div class="masthead-indicia">
    <span><b>APR 70 PICTURES</b> QUARTERLY</span>
    <span>{indicia.isoDate}</span>
    <span>{indicia.offices}</span>
    <span>{indicia.printRun} · {indicia.reel}</span>
  </div>
</div>
