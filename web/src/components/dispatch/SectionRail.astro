---
interface Props {
  num: string
  title: string
  meta?: string
}

const { num, title, meta = '' } = Astro.props
---

<div class="section-rail">
  <span class="section-rail-num">{num}</span>
  <h2 class="section-rail-title">{title}</h2>
  <span class="section-rail-meta">{meta}</span>
</div>
